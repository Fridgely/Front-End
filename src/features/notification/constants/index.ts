export const AM_PM = ["오전", "오후"];
export const HOURS = Array.from({ length: 12 }, (_, i) => i + 1);
export const MINUTES = Array.from({ length: 60 }, (_, i) => i);
export const PICKER_ITEM_HEIGHT = 50;
